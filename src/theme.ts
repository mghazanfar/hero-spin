import { createTheme } from "@material-ui/core";

export const marvelTheme = createTheme({
    palette: {
      primary: {
        light: "#ffffff",
        main: '#000000',
      },
      secondary: {
        main: "#ffffff",
        light: '#000000',
      },
    },
  });